# 项目代理规则

用户是 UI／交互设计师。说明开发概念时，先用产品或设计语言解释影响，再补充必要的技术术语。

## `main` 发布门禁

本仓库是公开仓库。任何代理准备合并到 `main` 或执行 `git push origin main` 前，必须完成本节检查。检查对象是远端 `main` 与待发布 `HEAD` 之间的全部提交和文件，不只检查最后一个提交。

### 1. 锁定发布范围

1. 更新远端引用并确认工作区干净。
2. 检查 `origin/main..HEAD` 的提交列表，以及 `origin/main...HEAD` 的文件、行数和体积变化。
3. 逐项确认新增内容属于用户本次要求。分支如果夹带其他功能、历史审阅素材或来源不明的提交，停止合并并报告差异；优先选择性迁移需要的提交或文件。

完成条件：每个待发布提交和新增文件都能对应本次任务；没有意外批次、缓存、中间产物或工作副本。

### 2. 安全与隐私检查

检查当前差异和待发布提交历史，至少覆盖：

- API Key、令牌、密码、Cookie、私钥、`.env`、`data/map-config.js` 等凭据或本机配置；
- 内部工单链接、附件直链或 UUID、会话标识、私有项目名、内部工具与操作流程；
- 姓名、邮箱、电话、地址、用户画像、聊天内容和批准原话；
- `/Users/<name>/`、`C:\Users\<name>\` 等本机绝对路径；
- 截图中的地址栏、账号、通知、文件路径、访问令牌或其他界面隐私。

发现疑似凭据时，不在终端输出或回复中复述完整值。记录文件和行号，并停止 push。`gitignore` 只能防止未跟踪文件进入 Git，不能清除已经提交的内容。

完成条件：未发现凭据或个人敏感信息；内部标识均已删除或替换为不关联真实工作区的说明。

### 3. 素材与数据授权检查

用户附件、地图截图、网络图片、PDF、字体、地理数据和第三方导出文件默认不具备公开再分发授权。只有在仓库中记录了可验证的来源、许可证和再分发依据后，才允许进入 `main`。

无法确认授权时，只保留必要的哈希、公开来源链接、文字说明和自制派生数据。原图、地图截图和完整第三方文件移至私有制品库，不放入公开 Git 历史。OpenStreetMap 等数据必须保留规定的许可证和署名。

完成条件：每个第三方文件都有明确的公开再分发依据；没有仅凭“用户提供”或“网上可访问”而提交的素材。

### 4. 仓库体积与生成物检查

检查新增二进制文件、重复文件、截图、掩膜、逐点轨迹、构建输出和审阅目录。单个新增文件超过 1 MiB，或本次新增内容合计超过 10 MiB 时，必须先向用户说明用途和体积并取得明确确认。运行站点不需要的审阅源文件应保存在私有制品库。

完成条件：公开仓库只包含产品运行、维护、测试和必要追溯所需的最小文件集。

### 5. 推送前报告与停止条件

推送前向用户简要报告：

- 比较基线和待推送提交；
- 改动文件数、主要目录和新增体积；
- 安全、隐私和授权检查结果；
- 测试结果及未验证项目。

出现以下任一情况时停止 push，并请求用户决定：

- 发现真实或疑似凭据、个人敏感信息；
- 用户原图或第三方素材缺少公开再分发依据；
- 分支包含超出本次任务的提交或大量审阅产物；
- 远端 `main` 在检查后发生变化；
- 需要重写历史、强制推送或删除远端内容。

所有检查通过后，才允许推送。推送完成后，确认本地 `main`、`origin/main` 和预期提交一致。

## 已公开内容的处理

如果敏感内容已经推送，新增删除提交不能从历史中移除。代理应先停止继续传播，说明普通删除与历史清理的区别，再等待用户明确授权后执行历史重写、强制推送或附件撤销。历史清理后仍需提醒用户检查缓存、派生仓库和本地克隆。

## 自动化边界

`AGENTS.md` 约束代理行为，但不能阻止用户或其他工具直接 push。需要强制执行时，应另外配置 GitHub 分支保护、拉取请求必检、密钥扫描和 CI 发布审计；不得把本文件当作服务器端安全控制。
